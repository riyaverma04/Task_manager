<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <h1>this is written in ejs</h1>
    <p>changing it here to get to know it actually works like html....

    </p>
    <h2>here we can calculate anything by using <  % =   % ></h2>
    <p>i am adding 2+2 here <%= 2+2%></p>

    <form action="/create" method="post">
        <input type="text" name="title" class="w-[90%] rounded-md bg-slate-900 p-2 text-white outline-none m-2">
        <textarea name="content" id="content" class="countent outline-none resize-none text-white bg-slate-900 p-2 m-2 rounded-md w-[90%]"></textarea>
        <input type="submit" class="bg-teal-500	 text-white rounded-sm px-6 py-2 uppercase block m-2">
    </form>
    <div class="tasks flex gap-3 m-8">
        <% if(files.length > 0 ) {%>
        <%files.forEach(function(val) { %>
            <div class="task bg-zinc-900 text-white text-center p-5 w-72 text-ellipsis">
                <h1 class="tracking-tighter text-3xl"><%= val  %></h1>
                <div class="flex justify-between align-center">
                    <a href="/files/<%= val%>" class="text-blue-500 inline-block mt-5">Read more...</a>
                    <a href="/edit/<%= val%>" class="text-blue-500 inline-block mt-5">Edit</a>


                </div>
                
            </div>
            <% }) %>
        
        <% } else { %>
            <h2 class="text-zinc-500">No task yet.</h2>
        <% } %>
       

    </div>




    <!--script -->
    <script src="/javascripts/script.js"></script>
</body>
</html>